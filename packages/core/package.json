{
  "name": "@perimeter/core",
  "version": "0.1.0",
  "description": "Perimeter core runtime — orchestrator, execution engine, safety guard, rate governance, audit log, finding registry, and reporters (spec §2, §4, §6).",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "templates"],
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@perimeter/sdk": "workspace:*",
    "@apidevtools/swagger-parser": "^10.1.0",
    "jose": "^5.6.3",
    "undici": "^6.19.5",
    "ulid": "^2.3.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/sarif": "^2.1.7"
  }
}
